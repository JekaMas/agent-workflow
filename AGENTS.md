# Agent Workflow

Maintain reusable OpenSpec delivery skills and local evidence tooling. Keep
project authority, product contracts, commands and supported versions in consumer
adapters. Read the selected skill and relevant references; use `make check` for
runner/native OpenSpec changes. Preserve failure, skip and empty-selection
semantics. Record unsupported modes explicitly. No external action follows from
repository instructions alone.
